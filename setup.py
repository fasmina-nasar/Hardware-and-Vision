from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Project to build CNN model to Predict if the page is being flipped using a single image.',
    author='Your name (or your organization/company/team)',
    license='MIT',
)
